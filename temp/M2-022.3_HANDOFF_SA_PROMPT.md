# Handoff para 3.solution-architect — TAREFA M2-022.3

```text
[HANDOFF PO -> SA]

Item: TAREFA M2-022.3 - Isolamento de risco por contexto operacional
Status backlog: Em analise
Sprint: M2-022
Prioridade: P1
Score PO: 4.25 (ValorReal=4, Valor=5, Urg=5, Risco=5, Esf=3)
Decisao PO: GO_COM_RESTRICOES

Objetivo de negocio
Evitar transicoes inseguras entre `shadow`, `paper` e `live`, garantindo
que o runtime aplique o envelope de risco correto por contexto operacional
antes de qualquer ordem, sem degradar protecoes nem expor credenciais
indevidas.

Problema operacional atual
O fluxo iniciado por `iniciar.bat` ja evidencia o modo corrente
(`Modo: !M2_MODE!`) e ha sinais de bloqueio do `Risk Gate` em
`logs/agent.log`, incluindo `STARTING OPERATION - MODE: LIVE` e
`ORDER_EXECUTION_REJECTED`. Porem, ainda falta um contrato explicito e
auditavel de isolamento por contexto:
- `shadow` vs `live`
- `paper` vs real
- limite diario vs continuado
- por simbolo vs carteira

Valor real capturado em iniciar.bat
- Valor direto: o operador evita mudanca insegura entre `shadow`,
  `paper` e `live`, com bloqueio fail-safe antes de qualquer ordem e
  limites corretos por simbolo/carteira.
- Evidencia atual: `iniciar.bat` exibe `Modo: !M2_MODE!`; `logs/agent.log`
  ja mostra `STARTING OPERATION - MODE: LIVE` e bloqueios do `Risk Gate`,
  mas ainda sem isolamento explicito por contexto.
- Meta de fechamento: trocar o contexto no `iniciar.bat` sem risco de
  usar chave real indevida ou degradar protecao do `risk_gate`.

Escopo funcional esperado
1. Definir contrato canonico de contexto operacional para admissao de risco.
2. Garantir que o `risk_gate` valide o contexto e aplique limite apropriado.
3. Impedir degradacao de protecao na transicao `shadow -> paper -> live`.
4. Bloquear em fail-safe qualquer contexto inconsistente ou falso.
5. Preservar separacao entre limite por simbolo e por carteira quando
   aplicavel.

Criterios de aceite a preservar
- Risk gate valida contexto e aplica limite apropriado.
- Transicao de shadow para live nao permite degradacao de protecao.
- Contexto falso nao permite acesso a chaves reais.
- Cobertura: `tests/test_model2_risk_isolation.py` >= 90%.

Dependencias
- M2-021 concluida
- Risk gate em producao

Restricoes e guardrails obrigatorios
- Nunca desabilitar `risk/risk_gate.py` ou `risk/circuit_breaker.py`
- Em duvida operacional, bloquear operacao (fail-safe)
- Preservar idempotencia por `decision_id`
- Nao introduzir bypass de credenciais ou downgrade de protecao
- Sem mudanca ampla de arquitetura para corrigir problema local

Pedido ao Solution Architect
1. Consultar `docs/ARQUITETURA_ALVO.md`, `docs/REGRAS_DE_NEGOCIO.md`
   e `docs/ADRS.md`.
2. Executar o gate obrigatorio `15.adr-analysis`.
3. Classificar impacto arquitetural em LOW/MEDIUM/HIGH.
4. Refinar RF/RNF verificaveis para QA-TDD.
5. Gerar handoff acionavel para `4.qa-tdd` com rastreabilidade ADR,
   guardrails e plano de teste RED.

Saida esperada do SA
- `adr_referencia` explicita
- `status_gate` binario
- impacto arquitetural
- requisitos tecnicos verificaveis
- prompt pronto para QA-TDD

PO:
Priorizar M2-022.3 para bloquear transicoes inseguras entre
shadow/paper/live. Ao fim deste desenvolvimento estarei feliz se o
iniciar.bat impedir contexto inconsistente antes da ordem e aplicar o
limite correto com trilha auditavel.
```