# Handoff QA-TDD — M2-022.3

```text
Voce e o agente 4.qa-tdd desta task.

Contexto da demanda:
- id: M2-022.3
- objetivo: isolar o risco por contexto operacional e bloquear combinacoes inseguras entre shadow, paper e live antes de qualquer ordem
- escopo_in: gate de admissao em live_execution.py; normalizacao de contexto em live_service.py; validacao preflight em go_live_preflight.py
- escopo_out: schema novo; bypass de risk_gate/circuit_breaker; mudanca ampla da arquitetura M2
- adr_referencia: ADR-002; ADR-004; ADR-007; ADR-009
- status_gate: APROVADO_POR_ADR
- impacto_arquitetural: MEDIUM

Requisitos refinados:
1. aceitar apenas `execution_mode` canonico em `shadow`, `paper` ou `live`, bloqueando qualquer valor fora do contrato com fail-safe auditavel.
2. impedir que `shadow` ou `paper` usem credenciais/contexto de `live`; em inconsistência, bloquear com `reason_code` explicito e sem envio de ordem real.
3. diferenciar o envelope de risco por contexto: `shadow` sem ordem real, `paper` com validacao de risco e credenciais testnet/paper, `live` com validacao completa de saldo e guardrails.
4. preservar separacao entre limite por simbolo e por carteira, incluindo contagem diaria e escopo por `execution_mode`.
5. manter `decision_id`, `reason_code`, `severity` e `recommended_action` na trilha auditavel de qualquer bloqueio.
6. implementar sem alteracao de schema e sem desabilitar `risk_gate` ou `circuit_breaker`.

Arquitetura e integracao:
- componentes: core/model2/live_execution.py; core/model2/live_service.py; core/model2/shadow_load_validation.py; scripts/model2/go_live_preflight.py; config/settings.py
- extensoes: evaluate_live_execution_gate; _snapshot_guardrail_state; build_config; validate_risk_context_isolation
- guardrails: risk_gate=ATIVO; circuit_breaker=ATIVO; decision_id=IDEMPOTENTE

Modelagem de dados:
- entidades: signal_executions; technical_signals; audit trail de execucao
- schema: sem_alteracao
- compatibilidade: sim; legado shadow/live preservado com `paper` explicitado no contrato

Plano de implementacao orientado a testes:
1. RED para rejeicao de contexto inconsistente (`shadow` com chave live, `paper` sem credencial adequada, modo invalido)
2. GREEN minimo para normalizar o contexto e diferenciar `shadow`, `paper` e `live` no gate
3. RED/GREEN para limites por simbolo/carteira e auditoria do bloqueio
4. REFACTOR mantendo verde

Suite de testes minima obrigatoria:
- unitarios: 6 casos em `tests/test_model2_risk_isolation.py`
- integracao: 3 casos cobrindo `go_live_preflight.py` e `live_service.py`
- regressao_risco: 3 casos garantindo que `risk_gate` e `circuit_breaker` continuam ativos

Criterios de aceite objetivos:
- [ ] `pytest -q tests/test_model2_risk_isolation.py` verde com cobertura >= 90%
- [ ] `paper` deixa de cair em `unsupported_execution_mode` e contexto inconsistente bloqueia antes da ordem
- [ ] Guardrails de risco preservados.

Comandos de validacao:
- pytest -q tests/test_model2_risk_isolation.py
- pytest -q tests/test_model2_go_live_preflight.py -k "paper or shadow or live"
- mypy --strict core/model2/live_execution.py core/model2/live_service.py scripts/model2/go_live_preflight.py

Entregaveis esperados:
- Lista de arquivos alterados
- Resumo de risco de regressao
- Evidencia de testes executados e resultado
```
