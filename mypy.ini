[mypy]
python_version = 3.11
# Excluir diretórios de checkpoints/binários que quebram a verificação de encoding
exclude = checkpoints(/|$)

# Evita reporte duplicado quando usamos --strict na linha de comando
# (mas mantemos configurações seguras caso a CLI não passe --strict)
strict = True
